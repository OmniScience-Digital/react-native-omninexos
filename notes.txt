You build and install it like a normal Android app after prebuilding:

**Steps:**

1. Prebuild native project

   ```bash
   npx expo prebuild --clean
   ```

2. Plug in your phone (USB debugging ON)

3. Run/install on device

   ```bash
   npx expo run:android
   ```

That will **build and install the APK automatically** on your phone.

---

**If you want a standalone APK file:**

```bash
cd android
./gradlew assembleRelease
```

APK will be here:

```
android/app/build/outputs/apk/release/app-release.apk
```

Then install manually:

```bash
adb install -r android/app/build/outputs/apk/release/app-release.apk


//play store

npx eas build -p android


//zip files

zip -r react-native-omninexos.zip . \
-x "node_modules/*" \
-x ".git/*" \
-x ".expo/*" \
-x "android/.gradle/*" \
-x "android/build/*" \
-x "android/app/build/*" \
-x "android/app/.cxx/*" \
-x "ios/build/*" \
-x "ios/Pods/*" \
-x "ios/DerivedData/*" \
-x "coverage/*" \
-x "*.log" \
-x "**/.DS_Store" \
-x ".env" \
-x ".env.*"

